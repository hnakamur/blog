<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: octopress | hnakamur's blog at github]]></title>
  <link href="http://hnakamur.github.com/blog/categories/octopress/atom.xml" rel="self"/>
  <link href="http://hnakamur.github.com/blog/"/>
  <updated>2013-03-24T01:44:21+09:00</updated>
  <id>http://hnakamur.github.com/blog/</id>
  <author>
    <name><![CDATA[Hiroaki Nakamura]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[octopressでgithubにブログ開設]]></title>
    <link href="http://hnakamur.github.com/blog/2012/03/03/how-to-setup-octopress/"/>
    <updated>2012-03-03T18:34:00+09:00</updated>
    <id>http://hnakamur.github.com/blog/2012/03/03/how-to-setup-octopress</id>
    <content type="html"><![CDATA[<p>rbenvとoctopressをインストールしてgithubにブログを作る手順のメモです。</p>

<h2>rbenvをインストール</h2>

<ul>
<li><a href="https://github.com/sstephenson/rbenv#section_2.1">sstephenson/rbenv</a></li>
<li><a href="https://github.com/sstephenson/ruby-build">sstephenson/ruby-build</a></li>
</ul>


<p>を参考にインストールしました。</p>

<p>インストール先は~/.rbenvとしました。</p>

<p>```
cd
git clone git://github.com/sstephenson/rbenv.git .rbenv</p>

<p>echo 'export PATH="$HOME/.rbenv/bin:$PATH"' >> .bash_profile
echo 'eval "$(rbenv init -)"' >> .bash_profile</p>

<p>. ~/.bash_profile
```</p>

<p>ruby-buildをインストールします。なお、私はHomebrewを使っているので/usr/localには書き込み権限をつけてあります。</p>

<p><code>
cd
git clone git://github.com/sstephenson/ruby-build.git
cd ruby-build
./install.sh
</code></p>

<p>rbenvでruby 1.9.2-p290をインストール。
<code>
rbenv install 1.9.2-p290
</code></p>

<p><code>
$ rbenv versions
* 1.9.2-p290 (set by /Users/hnakamur/octopress/.rbenv-version)
</code>
でインストールされたrubyがrbenvで認識されたことを確認。</p>

<p><code>
rbenv global 1.9.2-p290
</code>
で切替。</p>

<p><code>
$ which ruby
/Users/hnakamur/.rbenv/shims/ruby
$ ruby --version
ruby 1.9.2p290 (2011-07-09 revision 32553) [x86_64-darwin11.3.0]
</code></p>

<h2>octopressでgitにブログをセットアップ</h2>

<p><a href="http://octopress.org/docs/deploying/github/">Deploying to Github Pages - Octopress</a>
を参考にセットアップしました。</p>

<p>ここでは~/octopressを作業ディレクトリとしました。</p>

<p><code>
cd
git clone git://github.com/imathis/octopress.git octopre
cd octopress
ruby --version
gem install bundler
rbenv rehash
rake install
</code></p>

<p>ブラウザでgithubを開き、自分のアカウント(私の場合はhnakamur)でblogというプロジェクトを作ります。ブラウザで https://github.com/hnakamur/blog を開き、"Next steps:"に
<code>
git remote add origin git@github.com:hnakamur/blog.git
</code>
と書かれている行のgitのURL(ここではgit@github.com:hnakamur/blog.git)をコピーします。</p>

<p>その後
<code>
rake setup_github_pages
</code>
を実行し、"Enter the read/write url for your repository:"というプロンプトが出たらURLを貼り付けます。
<code>
Enter the read/write url for your repository: git@github.com:hnakamur/blog.git
</code></p>

<p><code>
vi _config.yml
</code></p>

<p>ブログタイトルなどを編集します。
<code>text _config.yml
url: http://hnakamur.github.com/blog
title: "hnakamur's blog at github"
subtitle: my trial and error log
author: Hiroaki Nakamura
…(略)…
date_format: "%Y-%m-%d"
…(略)…
</code></p>

<p>さらに
<code>text _config.yml (before edit)
permalink: /blog/:year/:month/:day/:title/
…(略)…
category_dir: blog/categories
…(略)…
titlecase: true       # Converts page and post titles to titlecase
</code>
を以下のように変更します。
<code>text _config.yml (before edit)
permalink: /:year/:month/:day/:title/
…(略)…
category_dir: categories
…(略)…
titlecase: false      # Converts page and post titles to titlecase
</code>
これは記事のパーマリンクやカテゴリページのURLが/blog/blog/…のようになってしまうのを/blog/…のようにするためです。</p>

<p>またタイトルの先頭を大文字にするのもオフにしています。</p>

<p>なお、アーカイブのページのURLは/blog/blog/archiveになってしまっていますが、変更方法をまだ見つけられていません。</p>

<h2>記事の作成と投稿</h2>

<p><code>
rake new_post['Hello, octopress!']
</code>
と実行すると、最後に
<code>
Creating new post: source/_posts/2012-03-03-hello.markdown
</code>
と作成されたファイルが表示されます。</p>

<p>エディタでこのファイルを開いて記事を書きます。
<code>
vi source/_posts/2012-03-03-hello.markdown
</code></p>

<h2>```</h2>

<p>layout: post
title: "Hello, octopress!"
date: 2012-03-03 17:57
comments: true</p>

<h2>categories: octopress</h2>

<p>octopressでブログ記事を投稿してみるテスト。
```</p>

<p><code>
rake generate
</code>
でHTMLが生成されます。</p>

<p><code>
rake preview
</code>
を実行して、ブラウザで
http://localhost:4000/blog/
を開くと、プレビューできます。</p>

<p><code>
rake deploy
</code>
でgithubにプッシュされ、10分ぐらい待つと
http://hnakamur.github.com/blog/
に記事のページで作られました。</p>

<p>時間がかかるのは初回にgithub pagesを作るときだけで、次回以降は即座に作られました。</p>

<h2>記事のソースをgithubにプッシュ</h2>

<p>リモートの設定にoriginを追加して、そこをデフォルトのブランチに設定。
<code>
git remote add origin git@github.com:hnakamur/blog.git
git config branch.master.remote origin
</code></p>

<p>設定の変更と生成されたソースをコミット、プッシュします。
<code>
git add .
git commit -m 'Modify config. Add source and sass'
git push origin HEAD:source
</code></p>

<p>github上でのブランチ名をsourceではなくmasterにしてよければ、単に
<code>
git push
</code>
でOKです。</p>

<h2>ローカルのApacheでプレビュー</h2>

<p>Apacheは事前にセットアップ済みという前提で、プレビュー用の設定を追加します。</p>

<p><code>
sudo vi /etc/apache2/other/blog.conf
</code>
と実行して、以下の内容で作成します(hnakamurの部分は自分のアカウント名で置換してください)。
```
Alias /blog /Users/hnakamur/octopress/public/blog
<Directory /Users/hnakamur/octopress/public/blog></p>

<pre><code>AllowOverride None
</code></pre>

<p>  DirectoryIndex index.html</p>

<pre><code>Order allow,deny
Allow from all
</code></pre>

<p></Directory>
```</p>

<p><code>
sudo apachectl restart
</code>
でApacheを再起動します。</p>

<p>これ以降は記事を編集してrake generateだけ実行すれば <a href="http://localhost/blog/">http://localhost/blog/</a> を開いてプレビューできます。</p>

<h2>今回のはまりポイント</h2>

<h3>YAMLで文字列を""で囲む必要があった</h3>

<p>```
$ rake generate
(in /Users/hnakamur/octopress)</p>

<h2>Generating Site with Jekyll</h2>

<p>unchanged sass/screen.scss
/Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/1.9.1/psych.rb:148:in `parse': couldn't parse YAML at line 16 column 13 (Psych::SyntaxError)</p>

<pre><code>from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/1.9.1/psych.rb:148:in `parse_stream'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/1.9.1/psych.rb:119:in `parse'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/1.9.1/psych.rb:106:in `load'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/1.9.1/psych.rb:205:in `load_file'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/gems/1.9.1/gems/jekyll-0.11.0/lib/jekyll.rb:119:in `configuration'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/gems/1.9.1/gems/jekyll-0.11.0/bin/jekyll:207:in `&lt;top (required)&gt;'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/gems/1.9.1/bin/jekyll:19:in `load'
from /Users/hnakamur/.rbenv/versions/1.9.2-p290/lib/ruby/gems/1.9.1/bin/jekyll:19:in `&lt;main&gt;'
</code></pre>

<p><code>
_config.ymlの
</code>
date_format: %Y-%m-%dT%k:%M:%S%z
<code>
を
</code>
date_format: "%Y-%m-%dT%k:%M:%S%z"
```
と修正して再実行したらOKでした。</p>

<h3>git push origin sourceでエラー</h3>

<p><code>
$ git push origin source
error: src refspec source does not match any.
error: failed to push some refs to 'git@github.com:hnakamur/blog.git'
</code></p>

<p>ググって <a href="http://stackoverflow.com/questions/959477/error-when-git-push-to-github">Error when "git push" to github - Stack Overflow</a> を発見。</p>

<p><code>
git push origin HEAD:source
</code>
にしたらOK。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Hello, octopress!]]></title>
    <link href="http://hnakamur.github.com/blog/2012/03/03/hello/"/>
    <updated>2012-03-03T17:57:00+09:00</updated>
    <id>http://hnakamur.github.com/blog/2012/03/03/hello</id>
    <content type="html"><![CDATA[<p>octopressでブログ記事を投稿してみるテスト。</p>
]]></content>
  </entry>
  
</feed>
